export const upStyle = ({ commit }, toStyle) => {
  commit('upStyle', toStyle)
}
export const upChoice = ({ commit }, toChoice) => {
  commit('upChoice', toChoice)
}
export const menuNav = ({ commit }, path) => {
  commit('menuNav', path)
}
export const upCate = ({ commit }, cate) => {
  commit('upCate', cate)
}
